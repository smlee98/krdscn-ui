import { ToggleSwitch } from "@/components/ui/krds/(selection)/toggle-switch";

export default function ToggleSwitchDisabledChecked() {
  return <ToggleSwitch label="스위치" disabled defaultChecked />;
}
